workflows:
  ios-dev:
    name: iOS Development Build
    instance_type: mac_mini_m2
    max_build_duration: 45
    environment:
      flutter: 3.41.x
    scripts:
      - flutter pub get
      - flutter build ipa --debug
    artifacts:
      - build/ios/ipa/*.ipa